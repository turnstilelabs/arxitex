# ArxiTex API

# Where the API writes graphs, definition banks, PDFs, anchors, logs, etc.
OUTPUT_DIR=./pipeline_output

# Optional: CORS allow-list (comma-separated)
# ALLOWED_ORIGINS=http://localhost:3000

# LLM providers (optional; required only for LLM features)
# OPENAI_API_KEY=
# TOGETHER_API_KEY=

"""Database schema + persistence helpers for ArxiTex.

This package is intentionally lightweight (sqlite3 stdlib) and keeps all tables
in the same SQLite database file as the existing indices (arxitex_indices.db).
"""
